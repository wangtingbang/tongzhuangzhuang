#好好学习HTML
* 后台的东西，等学完JS以后就可以直接用了，那时候页面就是活的了。

##create a new repository on the command line 
echo "# tongzhuangzhuang" >> README.md  
git init  
git add README.md  
git commit -m "first commit"  
git remote add origin git@github.com:wangtingbang/tongzhuangzhuang.git  
git push -u origin master  

##…or push an existing repository from the command line
git remote add origin git@github.com:wangtingbang/tongzhuangzhuang.git  
git push -u origin master  

